# 未决问题

已在 R5 部分关闭：nginx 对外链路形态、MCP filter 语义（黑名单名 `sandbox` 但 hub 无同名 server）、office edition 对 hijack 的影响、平台 Go 模块名、创作画布 CLI 方案名字符串。

## 仍未坐实

1. `CREATE_SANDBOX_PARAMS` 明文（ENCv1）
2. 外层网关一次性 token / 签名 URL / TTL
3. 对话框附件 → 容器路径的**实测**映射（仅有接口，本会话无附件）
4. Security review 规则服务位置
5. 出网带宽 / 连接数硬限
6. Seed 模型与 Work / 创作画布的编排粘合方式
7. 内置 Sandbox MCP 各工具完整契约与 skill 清单（进行中 H）
8. ComputerUse GUI 面 vs browser CDP 面的调用分流（进行中 I）
9. 日志 / trace 出沙箱路径（进行中 K）
10. standby 借出完整状态机与 profile 枚举（进行中 N）
