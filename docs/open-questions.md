# 未决问题

## 已关闭（本观测窗口）

- nginx 对外链路与信任内网模型
- MCP filter 语义；内置 Sandbox 工具表
- ComputerUse vs browser vs CC **三条通道存在性**
- standby / cold / PROFILE=`all|ci`
- office edition 对 hijack 的影响
- 上传默认落地 `/tmp/<filename>`；旧 attachments 批量接口已不在包中
- AIO / 创作画布 CLI / 镜像 v1.14.10 等指纹

## 仍未坐实

1. `CREATE_SANDBOX_PARAMS` 明文（ENCv1）
2. 外层网关一次性 token / 签名 URL / TTL
3. OTEL/日志 **导出端点** 与平台回收路径
4. `trajectory.jsonl` 出沙箱机制
5. GUI vs CDP vs CC 的 **场景调度策略**（平台侧）
6. Seed 模型与 Work / 创作画布 / AIO 的编排粘合
7. `/opt/skills` 与 `sandbox_load_skill`（AIO_SKILLS_PATH）为何未打通
8. 出网带宽 / 连接数硬限
9. 多租户隔离保证（除 Kata 外）与热池经济学
